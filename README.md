# GPT Vision Dummy Server

## Features
- Accepts image uploads
- Returns dummy JSON response
- No API key required

## To Run

```bash
npm install
npm start
```

POST to `http://localhost:10000/analyze` with form-data (key: image, file: your image)